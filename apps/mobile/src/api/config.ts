// Real dev API Gateway endpoint (infra/environments/dev, services/properties + services/accounts —
// see SPEC.md §6). Same pattern as ../auth/cognitoConfig.ts.
export const apiConfig = {
  baseUrl: "https://vhqpw6i2lh.execute-api.eu-west-1.amazonaws.com",
};
